# Python Pipelines Primer

This is a sample project building a CICD pipeline in Python with CircleCI. And we'll do it live.

- Run locally: `flask main.py`
- Run gunicorn server: `gunicorn main:app`


